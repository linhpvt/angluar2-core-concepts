import { AppConfig } from '.';

// Define a plain text javascript object
export const PROD_APP_CONFIG: AppConfig = {
  apiUrl: 'https://jsonplaceholder.typicode.com',
  cacheSize: 10,
  environment: 'development',
};
